name: Candidate delivery

on: [pull_request]

permissions:
  contents: read

jobs:
  candidate-delivery:
    name: Candidate delivery
    runs-on: ubuntu-latest
    steps:
      - name: Pass deterministic gate
        run: exit 0
